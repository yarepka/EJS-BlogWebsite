<%- include("./partials/header") -%>
<h1>Home</h1>
<p><%= homeStartingContent %></p>

<% posts.forEach((post) => { %>
    <% if(post.title!=="") %> <h1><%= post.title %></h1>
    <p><%= post.content %></p>
<% }); %>

<%- include("./partials/footer") -%>